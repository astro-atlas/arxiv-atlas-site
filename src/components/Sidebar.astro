---
/**
 * Sidebar — Navigation + auto-generated TOC.
 * Portal links are loaded from pages_index.json at build time.
 */
import pagesIndex from '../data/pages_index.json';

interface Props {
  currentPage?: string;
}
const { currentPage } = Astro.props;

// Find portal pages (page_slug starts with 'pages/portal-')
const portalPages = pagesIndex.filter((p: any) =>
  p.page_slug && p.page_slug.startsWith('pages/portal-')
).sort((a: any, b: any) => (a.title || '').localeCompare(b.title || ''));
---
<aside class="page-sidebar">
  <div class="sidebar-section">
    <div class="sidebar-heading">Contents</div>
    <ul class="sidebar-nav" id="toc-nav">
      <!-- Auto-populated by JS from page headings -->
    </ul>
  </div>
  <div class="sidebar-section">
    <div class="sidebar-heading">Navigation</div>
    <ul class="sidebar-nav">
      <li><a href="/">Home</a></li>
      <li><a href="/papers/">Paper Summaries</a></li>
      <li><a href="/random/">Random Page</a></li>
    </ul>
  </div>
  {portalPages.length > 0 && (
    <div class="sidebar-section">
      <div class="sidebar-heading">Portals</div>
      <ul class="sidebar-nav">
        {portalPages.map((p: any) => {
          const href = '/' + p.page_slug + '/';
          const label = (p.title || p.page_slug).replace(/^Portal:\s*/i, '');
          return <li><a href={href}>{label}</a></li>;
        })}
      </ul>
    </div>
  )}
</aside>
